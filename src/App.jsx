import React, {Component} from 'react';//component feature

class App extends Component {
    render( ) {//have to include the return statment
        return (
            <div>Coundown Champ, App Component</div>
        )
    }
}

export default App;
